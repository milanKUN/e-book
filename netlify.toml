[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  included_files = ["netlify/secure-data/ebook.pdf"]

# SPA Routing for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
